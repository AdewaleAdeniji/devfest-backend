export enum userRoles {
    ADMIN = "admin",
    USER = "user",
}
export enum signinMethod {
    LOCAL = "informanus-user-service",
    GOOGLE = "google",
}
export enum userType {
    COACH = "coach",
    FormUser = "formUser",
    CLIENT = "client",
    ADMIN = "admin",
}
export enum userStatuses {
    ACTIVE = "active",
    UNVERIFIED = "unverified"
}